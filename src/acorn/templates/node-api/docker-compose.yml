version: "3.8"
services:
  api:
    build: .
    ports:
      - "{{port}}:{{port}}"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - NODE_ENV=development
      - PORT={{port}}
